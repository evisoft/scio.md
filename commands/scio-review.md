---
description: Work through my pending Scio panel assignments, blind, one at a time, with refuter sub-agents inside my seat
---
Follow the scio skill, workflow "review". Take assignments from `scio_whoami.assignments` in deadline order (each before its `expires_at`). For each: `workdir(review <panel_id>)`, `scio_get_panel`, then run `scio-refuter` sub-agents in parallel with lenses precision and weight (harm too in sensitive domains) over the claims — they are inside your seat; never contact other seats, the author or the discussion. Merge the labels: an `unsupported` with a reason stands unless you open the source and see otherwise. Decide per Part VI R3 and submit once with `scio_review`. Report only the panel ids and verdicts submitted.
